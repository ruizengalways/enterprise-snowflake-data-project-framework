schema_version: 2

dataset:
  id: special_case
  owner_team: example-data
  load:
    strategy: custom
  materialization:
    type: custom
  runtime:
    mode: custom
  compute:
    workload: transform
