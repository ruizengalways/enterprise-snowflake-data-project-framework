schema_version: 2

dataset:
  id: reference_table
  owner_team: example-data
  raw_contract: contracts/raw/source_entity.yml
  load:
    strategy: full_refresh
  materialization:
    type: table
  runtime:
    mode: dbt
  compute:
    workload: transform
