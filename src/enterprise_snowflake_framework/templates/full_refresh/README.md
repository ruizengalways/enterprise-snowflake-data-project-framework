# __DATASET_ID__ — full-refresh Silver pattern

This pattern is for small or naturally replaceable trusted Silver relations. The domain SQL owns the exact `CREATE OR REPLACE` or transactional replace behavior.
