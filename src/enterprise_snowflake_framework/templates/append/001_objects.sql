-- __DATASET_ID__: create the trusted append target.
-- Copy the explicit column definitions from __RAW_CONTRACT__.

-- Example shape only; replace the TODO columns before deployment.
-- create table if not exists SILVER_CANONICAL.__ENTITY_UPPER__ (...);
