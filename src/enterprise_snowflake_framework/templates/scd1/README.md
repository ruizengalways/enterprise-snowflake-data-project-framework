# __DATASET_ID__ — SCD1 Silver pattern

Use one deterministic latest source row per business key, keyed MERGE for non-delete rows, and an explicit tombstone delete step. The generated source files belong to the domain repository and should be edited there.
